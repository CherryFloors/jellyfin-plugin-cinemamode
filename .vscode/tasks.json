{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jellyfin Plugin Build",
            "type": "shell",
            "command": "dotnet publish --configuration Release",
            "problemMatcher": "$msCompile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
    
}
